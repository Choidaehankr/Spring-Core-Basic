<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="example.mybatis.repository.NewsRepository">
  <!-- 회원 가입 -->
  <insert id="save1" parameterType="example.mybatis.domain.News">
    insert into news(title, author, page) values (#{title}, #{author}, #{page})
  </insert>
</mapper>